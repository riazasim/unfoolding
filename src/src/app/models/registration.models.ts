export interface DeaRegistrationModel {
  fullName: string;
  email: string;
  contactNumber: string;
}

export interface DeaRegistrationResponseModel {
  id: number;
  fullName: string;
  email: string;
  contactNumber: string;
}
